# Copy to `.env` and update values before running `docker-compose up`.
# NOTE: Do NOT commit real secrets to the repository. This example mirrors values
# present in `src/main/resources/application.properties` for local testing.
#
# SECURITY: This file (.env.example) is safe to version control.
#           Your actual .env file is in .gitignore and should never be committed.

# ============================================
# MySQL Database Configuration (DB_A)
# ============================================
MYSQL_ROOT_USERNAME=root
MYSQL_ROOT_PASSWORD=Evertec.2025
MYSQL_DATABASE=spring_batch_db

# ============================================
# PostgreSQL Database Configuration (DB_B)
# ============================================
POSTGRES_USER=root
POSTGRES_PASSWORD=Evertec.2025
POSTGRES_DB=spring_batch_db

# ============================================
# Spring Boot Batch Configuration (Optional)
# ============================================
# These are automatically set in docker-compose.yml but can be overridden here
# SPRING_PROFILES_ACTIVE=docker
# SPRING_BATCH_JOB_ENABLED=false
# SPRING_BATCH_JDBC_INITIALIZE_SCHEMA=always
